exclude_paths:
  - '/vendor/*'

engines:
  phpcodesniffer:
    enabled: true
    config:
      file_extensions: 'php'
      standard: 'PSR1,PSR2'
  phpmd:
    enabled: true
    config:
      file_extensions: 'php'
      rulesets: 'cleancode,codesize,design,naming,unusedcode'

ratings:
  paths:
    - '**.php'
